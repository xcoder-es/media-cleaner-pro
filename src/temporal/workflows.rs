pub struct StandaloneWorkflow;

impl StandaloneWorkflow {
    pub fn new() -> Self {
        StandaloneWorkflow
    }
}

impl Default for StandaloneWorkflow {
    fn default() -> Self {
        Self::new()
    }
}
